<div>
  <h2>Flats</h2>
  <%= link_to "New flat", new_flat_path%>
</div>

<div class="container">
  <div class="w-50">
    <ul>
      <% @flats.each do |flat| %>
      <li>
        <%= link_to flat.name, flat_path(flat) %><br>
      </li>
      <% end %>
    </ul>
  </div>
</div>
